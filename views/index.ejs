<!DOCTYPE html>
<html lang="en">
    
    <!-- This is my heading now -->
    <%- include ('./partials/head.ejs', {page: 'All products'}) %>
    
    <!-- This is my nav bar now -->
    <%- include ('./partials/nav.ejs') %>
    <section class="hero is-medium has-background-light">
        <div class="hero-body">
            <p class="title">All Cars</p>
        </div>
    </section>
    
    <main class="container is-max-widescreen">
        <div class="columns is-multiline p-5">
            <% for(let idx = 0; idx < cars.length; idx++){ %> 
                <% const car = cars[idx] %>
                
                <div class="column is-one-third">
                    <article class="card">
                        <section class="card-image">
                            <figure class="image is-3by3">
                                <img src="<%= car.image %>" alt="Placeholder image" />
                            </figure>
                        </div>
                    </section>
                    <div class="card-content">
                        <div class="content">
                            <h3>
                                <a href="/cars/<%= car._id %>"><%= car.car_make %> </a>
                            </h3>
                            <h3>
                                <a href="/cars/<%= car._id %>"><%= car.car_model %> </a>
                            </h3>
                            <p>$<%= car.price %></p>
                        </div>
                    </div>
                    
                </article>
            </div>
            <% } %>
        </div>
    </main>
</body>
</html>